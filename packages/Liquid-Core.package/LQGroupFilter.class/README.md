A LQGroupFilter Filters answer sets by user group. Keeps a set of excluded groups that can be toggled and answers whether a given group is excluded. Used by the visualization to restrict results to selected groups.

Instance Variables
	excludedGroups:	<Set of: String> group names currently excluded from the results
