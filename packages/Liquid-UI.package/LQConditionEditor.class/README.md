A LQConditionEditor is Stateless UI helper that runs the dialogs for adding and removing conditions on questions of a poll draft. Extracted from LQPollDraftBuilder to keep the builder focused on constructing the draft itself.

Instance Variables
