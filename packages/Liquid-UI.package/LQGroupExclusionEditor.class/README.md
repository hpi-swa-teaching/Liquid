A LQGroupExclusionEditor runs the interactive workflow for excluding user-set groups from a choice. It pops the modal pickers (which choice, which groups) and applies the selected groups to the chosen choice. It is stateless: the poll-draft and the relevant question builder are passed in per call. addExclusionFor:questionBuilder: answers a Boolean indicating whether an exclusion was applied.