A LQGroupExclusionEditor is Stateless UI helper that runs the dialogs for excluding user groups from questions of a poll draft. Extracted from LQPollDraftBuilder to keep the builder focused on constructing the draft itself.

Instance Variables
