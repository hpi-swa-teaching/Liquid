A LQExportFormatTests is a class containing the tests for the class LQExportFormat: it is testing whether a single field, a row and a whole table are turned into correctly formatted output text.

Instance Variables
